extends layout 

block content 
    h1= title 
    ul 
        for category in category_list 
            li.text-item 
                a(href=category.url) #{category.name}
        else 
            li.text-item No categories found