extends layout 

block content 
    h1= title 
    ul 
        for product in product_list 
            li 
                a(href=product.url) #{product.name}
                p= product.brand.name
        
        else 
            li No products found